node_modules
dist
.DS_Store
server/public
vite.config.ts.*
*.tar.gz

# Sensitive files
user_credentials.csv
.env
.env.*
*.pem
*.key
# Planned-but-unwired scaffolding (job queue / redis) — not yet in scope
_planned/

