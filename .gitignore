node_modules
dist
.DS_Store

# Transient Claude Code agent worktrees (auto-created/cleaned per session)
.claude/worktrees/
server/public
vite.config.ts.*
*.tar.gz

# Sensitive files
user_credentials.csv
.env
.env.*
*.pem
*.key