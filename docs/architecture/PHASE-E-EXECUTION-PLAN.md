# Phase E — Enterprise Architecture Transformation: Execution Plan

**Status:** In progress · **Strategy:** incremental (strangler-fig), never big-bang.

This is the concrete, sequenced plan for evolving SALIS-GMS from a partially
modular monolith into a **Domain-Driven Modular Monolith**. It is designed to be
executed one domain at a time, keeping the build, tests, and production behavior
green after every step.

## Guiding constraints

1. **Preserve public API behavior** unless a breaking change is explicitly
   approved. Existing endpoints keep their exact wire shapes during migration.
2. **Migrate one domain at a time.** Each domain is validated independently.
3. **Remove legacy code only after parity is confirmed** by tests.
4. **Every step compiles, lints, and passes tests** before it is committed.

## Target layout (north star) vs. current landing

The vision uses top-level `apps/`, `packages/`, `modules/`, `infrastructure/`.
Reaching that literal layout requires moving the repo to a workspace/monorepo
build — itself a later phase. To make progress **without** a big-bang build
change, the same architecture lands under `server/` today and is relocated once
the monorepo tooling is in place:

| North-star            | Lands today as                         |
| --------------------- | -------------------------------------- |
| `infrastructure/*`    | `server/infrastructure/*`              |
| `packages/permissions`| `server/infrastructure/permissions`    |
| `packages/validation` | `server/modules/*/validators` + Zod    |
| `modules/<domain>/*`  | `server/modules/<domain>/*`            |
| `apps/{api,worker,…}` | future — extract once modules are clean |

## What is delivered now (foundation + reference domain)

### Foundation — `server/infrastructure/`
- **errors/** — `DomainError` hierarchy (E12): NotFound / Validation / Auth(z) /
  BusinessRule / Conflict / Infrastructure, each with code, HTTP status,
  details, correlation id, and structured context.
- **http/response.ts** — standard success/error envelope + safe error mapping
  (E10). Adopted for new/opt-in endpoints; existing contracts preserved.
- **events/event-bus.ts** — in-process event bus (E7): idempotent delivery,
  retry-with-backoff, dead-letter capture, per-subscriber isolation.
- **di/** — typed DI container + composition root (E3).
- **repository/base-repository.ts** — repository contracts (E4).
- **permissions/registry.ts** — central RBAC registry with role grants,
  policy overrides, tenant/ownership guards, and an Express `authorize` guard (E8).

### Reference domain — `server/modules/customers/`
The copyable template every future domain follows. Layered per E2:

```
customers/
  controllers/   # thin HTTP adapters; no business rules, no data layer
  services/      # business rules, tenant scoping, events (E5)
  repositories/  # the only data-layer access (E4) — wraps `storage` (seam)
  validators/    # Zod schemas (E9)
  domain/        # entity/domain types
  dto/           # wire-shape mappers (E10)
  events/        # published event contract + handlers (E7)
  __tests__/     # unit tests (DB-free)
  index.ts       # DI assembly → Express Router
```

Dependency direction is enforced: `controller → service → repository → storage`.
The customer repository **delegates to the legacy `storage` facade** — a
strangler-fig seam that establishes the boundary now; its internals move to
direct Drizzle queries later with no caller change. Behavior is identical to the
retired `server/routes/customers.ts`.

## Governance (E14)

- `scripts/check-architecture.mjs` (`npm run lint:arch`) fails CI if a controller
  or service imports the data layer directly, or a module imports another
  module's repository.
- ESLint `no-restricted-imports` surfaces the same at edit time.
- ADRs under `docs/adr/` record significant decisions.

## Domain migration sequence (repeatable playbook)

For each domain: **Extract → Compile → Test → Verify parity → Commit.**

1. customers ✅ (reference)
2. vehicles ✅ (list + `/vehicles/:id/*` maintenance reads; `requireResourceOwnership` preserved)
3. garage ✅ (garages list/detail/branches + role catalog; `requireManagerOrAbove` / `requireResourceOwnership` preserved) · appointments ✅ (tenant-pinned list + by-id 404)
4. jobcards ✅ (read surface: list + detail/details + parts/tasks; `/parts`
   Drizzle query absorbed into the repository) → estimates ✅ (**first monolith
   extraction**: 9 endpoints — reads, writes, stats SQL, and the two conversion
   workflows; `estimate.converted_to_{job_card,invoice}` events wired through the
   bus — the first write-path events) → invoices ✅ (**8 endpoints** incl. the
   184-line server-side `from-job` calculation across 8 tables, status-transition
   workflow, and role-gated delete; `invoice.created` events on every creation
   path — the head of the `InvoiceCreated → …` chain; non-contiguous monolith
   removal preserving the reconciliation handler) → payments ✅ (garage-scoped
   join list, atomic `recordPayment` create and role-gated `reversePayment`
   delete; `payment.received` / `payment.reversed` events; gateway + supplier
   payments left as separate domains) — **financial core complete**
5. inventory ✅ (items core: spare-parts list/by-id + per-garage inventories; **dashboards**: overview/items/low-stock/reorder/suppliers/turnover/valuation — repository owns all aggregation SQL + PO writes, service owns the analytics math, graceful-degradation defaults preserved; **stock-alerts** (4), **inventory-audit-trail** (2), **inventory-transfers** (6, dual-garage from/to ownership) ✅ — non-contiguous monolith removal keeping reorder-settings/pricing-history) / procurement / **suppliers** ✅ (core: suppliers list/by-id + supplier price lists list/by-id/compare; purchase-orders + purchase-tasks, deliveries, reorder-settings, pricing-history ✅ — extracted into a `procurement` module; PO/PO-item/task **writes** (create/with-items/update/delete, incl. the parent-scoped item-delete ownership guard and the task-parts loop) migrated too — procurement fully owns its surface)
6. crm ✅ (customer-360 list/detail, segments, loyalty summary + award-points, retention analytics, campaigns — raw-SQL read model moved wholesale into a `crm` module: repository owns every `db.execute(sql…)`, service owns the tier/segment/loyalty/retention math, controller keeps the garage-required 403 and graceful-degradation defaults) · insurance ✅ (claims create/list, status update with `requireResourceOwnership`, trailing-year analytics — first monolith extraction delegating through the `phase6-compliance-service` facade; DTO→persistence mapping in the service, `validatePatchBody` boundary in the controller) · fleet ✅ (fleet-accounts surface: account list/detail/create, vehicles, maintenance schedule, analytics roll-ups — extracted from `server/routes/fleet.ts`; the account/vehicle view mappers + tenant-scope filter + analytics math in the service, `resolveGarageScope` + create-validation boundary in the controller; the monolith fleet-management block ✅ (25 endpoints — groups/vehicles/contracts/pricing-tiers/maintenance-schedules CRUD in a dedicated `fleet-management` module, all parent-scoped `requireResourceOwnership` guards + `/group/:fleetGroupId`-before-`/:id` ordering preserved, non-contiguous removal) and the fleet-tracking block ✅ (12 endpoints — vehicle telemetry (locations), geofence zones/events, route planning; the per-vehicle tenant-ownership checks + geofence/route 404/403 rules moved into the service as domain errors, the `requireResourceOwnership` guards on the geofence/route mutations preserved; contiguous removal) — **fleet fully modularized**) · marketplace ✅ (public provider-discovery reads: providers list/detail, smart `/find` search, provider reviews — non-contiguous monolith removal; the four intentionally-unauthenticated reads delegate to `storage`, service holds the 404 + review cap, controller keeps the `/find` min-length 400; the authenticated write-path ✅ (eBay/Amazon parts search + order placement/tracking + the still-stubbed order listing + the transaction-gated `/my/reviews` submission — extracted into a `marketplace-writes` sub-domain: the repository is the sole data/external-service seam (the `phase3Service` marketplace integration + the `storage` review reads/writes), the service owns the garage-stamping on order placement and the review guards as `ValidationError`/`NotFoundError`/`AuthorizationError` domain errors, the controller keeps the `201`s + the per-handler `{ message }` 500 bodies + the 400/404/403 review mapping; the five monolith handlers removed — including a **dead duplicate** `POST /api/marketplace/orders` shadowed by the live handler, dropped in the extraction — all behind `isAuthenticated`; the provider-reviews integration test passes unchanged as the behavioral safety net) — **marketplace fully modularized**)
7. hr ✅ (hr-payroll surface: employee directory (raw SQL, manager+ gated), employee detail/create with Saudi-compliance (GOSI/end-of-service/vacation) + duplicate-email 409, attendance synthesis, leave-request workflow, payroll summary + pay slip (ADMIN/MANAGER/ACCOUNTANT gated) — extracted from `server/routes/hr-payroll.ts`; repository owns the raw SQL + leave storage, service owns the compliance/payroll math with domain errors, controller keeps the role guards + graceful-degradation defaults) · reports ✅ (management reporting: revenue (VAT-exclusive, groupBy day/week/month), technician-performance, inventory-turnover, customer-analytics, executive-summary — extracted from `server/routes/reports.ts`; repository owns the aggregate SQL, service owns the groupBy→TO_CHAR mapping + summary number extraction, controller keeps the financial/management role guards + session-garage 403) · analytics ✅ (full `/api/analytics/*` surface — performance report (target synthesis/retention/MoM + management fail-closed 403), dashboard-metrics, custom-reports stubs, profit-analysis, customer-LTV, heat-maps; consolidated the `analytics-performance.ts` route file + 7 monolith handlers into one `analytics` module; repository delegates to the `analytics-service`/`business-intelligence` facades + the two direct performance queries, service owns the snake→camel transforms + roll-ups) · ai ✅ (consolidated the three AI route files — business insights + revenue/demand forecasts, the PRO-gated statistical predictions/accuracy (day-of-week moving average + MAPE), and the ENTERPRISE-gated LLM repair-guide with deterministic preset fallback — into one `ai` module; repository owns the `business-intelligence` helpers + vehicle lookup + the OpenAI completion, service owns the demand-series/branching/MAPE math + repair-guide orchestration, controller keeps the garage 403 / Zod 400 and the `requirePlan` gates live on the routes; the monolith `/api/ai/*` endpoints are being folded in incrementally — **job-estimations** ✅ (estimate-job + list/detail/patch: repository owns the `estimateJobTime` LLM helper + the storage CRUD, service owns the estimate assembly + the 404/403 ownership rules + the garage-change 403, controller keeps the `insertAIJobEstimationSchema.partial()` 400 via `sanitizeZodError`; the four monolith handlers removed, `requireResourceOwnership({table:'ai_job_estimations'})` preserved on the `:id` routes); **maintenance-predictions** ✅ (predict-maintenance + predictive-diagnostics + list/detail/acknowledge + the batch analyze: repository owns the three engines (`predictMaintenance`/`generatePredictiveDiagnostic`/`analyzePredictiveMaintenance`) + the storage CRUD + the vehicle/job-card reads, service owns the prediction assembly + the analyze VIN-match/service-history loop + the 404/403 ownership + acknowledge transition, controller thin; the six monolith handlers + their now-unused imports removed, `requireResourceOwnership` preserved on the `:id` routes); **parts-recommendations** ✅ (recommend-parts + list/detail/patch: repository owns the `recommendParts` LLM helper + storage CRUD, service owns the assembly + 404/403 ownership + garage-change 403, controller keeps the `insertAIPartsRecommendationSchema.partial()` 400; monolith handlers + unused imports removed, `requireResourceOwnership` preserved on `:id`); **schedule-optimizations** ✅ (optimize-schedule + list/detail/patch: repository owns the `optimizeSchedule` LLM helper + storage CRUD, service owns the assembly + 404/403 ownership + garage-change 403, controller keeps the `insertAIScheduleOptimizationSchema.partial()` 400; monolith handlers + unused imports removed, `requireResourceOwnership` preserved on `:id`); **chat** ✅ (customer chat: POST /ai/chat + chat-conversations list/detail/handoff: repository owns the `chatWithCustomer` LLM helper + storage conversation CRUD, service owns the conversation resolve/create + chat orchestration + message-append + handoff-status transition + 404/403 ownership, controller keeps the message-required 400 + the new-conversation `ZodError`→`sanitizeZodError` 400; monolith handlers + the now-unused `./ai` chat import removed, `requireResourceOwnership` preserved on `:id`); **ocr-documents** ✅ (document OCR: list/upload/detail/patch: repository owns the storage OCR CRUD + the placeholder mock-extraction generator — the external-OCR seam holding the date/random nondeterminism, service owns the document assembly + defaults + the not-found 404 + update defaults, controller thin; monolith handlers removed, the parent-scoped `requireResourceOwnership({table:'ocr_documents', parent:{table:'vehicles', fk:'vehicle_id'}})` preserved on `:id`) — **the AI feature set is now fully modularized: all 26 monolith `/api/ai/*` endpoints live in `server/modules/ai`**) · platform ✅ (administration surface: the per-garage feature-flag CRUD — list/detail/create/patch/delete — extracted from `server/routes/feature-flags.ts` into a `platform` module; the repository owns the tenant-scoped Drizzle queries, the service owns the existence/validation rules as `NotFoundError`/`ValidationError` domain errors, the controller keeps the legacy status codes + `{ message }` bodies with auth enforced by the route middleware; **backup** ✅ (admin backup surface: status/create/list/export — extracted from `server/routes/backup.ts` into the `platform` module as a second sub-domain; the repository owns the `storage` backup-history methods + the direct Drizzle table-count and per-type export queries, the service owns the dev-snapshot math (counts→records→estimated size) + the unknown-export-type `ValidationError`, the controller keeps the `{ error }` 500/400 bodies + the export download headers, all four routes `isAuthenticated` + `requireAdmin`); **subscriptions** ✅ (billing surface: public plan catalog + per-garage current/change-plan/cancel/resume + platform-admin list/patch — extracted from `server/routes/subscriptions.ts` into a `subscriptions` module; the repository delegates the `storage` ensure/update/list, the service owns the catalog composition + plan-change period math + Stripe-readiness note + cancel-at-period-end transition, the controller keeps the garage/platform-admin 403s + the Zod 400s and reads `STRIPE_SECRET_KEY`)) · provider ✅ (the caller's garage acting as a marketplace provider — the twelve `/api/provider/*` handlers across bookings (list + status patch), offerings (list/create/patch/delete), public profile (get/patch), product orders (list + status patch) and insurance quotes (list + respond) — non-contiguous monolith removal, interleaved with the customer-side `/api/my/*` surface that stays; the repository is the sole `storage` seam (incl. the best-effort notification writes), the service owns the "No provider account associated" 403 + the per-surface status validations + the not-found 404s + the profile allow-list + the trim/allow-list rules as `AuthorizationError`/`ValidationError`/`NotFoundError` domain errors + the best-effort customer notifications fired after a state change, the controller keeps the `insertProviderOfferingSchema` Zod 400s at the boundary (`sanitizeZodError`, checked AFTER the provider guard to preserve the legacy 403-before-400 ordering) + the `201`s + the exact per-handler `{ message }` 500s; the provider-only `notifyCustomer` helper moved into the module while the customer-side `notifyProviderAdmins` stays; all `requireResourceOwnership({tenantColumn:'provider_id'})` guards preserved on the `:id` mutations; the provider-offerings + marketplace-bookings integration suites pass unchanged as the behavioral safety net) · administration ✅ (platform-admin control plane: the fourteen `/api/platform-admin/*` handlers — cross-tenant stats (with live MRR from the plan mix), garages list/create/status, suppliers + support-tickets oversight, measured system-health, and the garage-onboarding + subscription-request review queues — non-contiguous monolith removal keeping the interleaved PUBLIC intake endpoints (`POST /api/garage-applications`, `POST /api/subscription-requests`) in place; the repository owns the direct Drizzle cross-tenant queries + the `storage` application/request/ticket methods + the system-introspection probes (DB ping, process metrics, integration env-config) + the temp-credential minting seam, the service owns the MRR roll-up + garage-name uniqueness/address composition + support-ticket allow-list + the application approve/reject workflow (temp-password minting when the applicant has none) + the subscription-request transitions (incl. the `/already/` → 409 translation) as `ValidationError`/`NotFoundError`/`ConflictError`, the controller preserves the raw `{message: error.message}` 500s + the fixed 500 strings + the `201`; every route keeps `requirePlatformAdmin` and the seven mutations keep `auditLog`; the SA-005/SA-008 source-contract tests were repointed at the module and the platform-admin/garage-application/subscription-request integration suites pass unchanged as the behavioral safety net) — broader administration follow-ons (per-garage system settings) remain) · call-center ✅ (the twenty-five `/api/call-center/*` handlers — queues (CRUD + with-members), queue-members (add/list/patch/delete), sessions (CRUD + assign), notes + recordings (parent-scoped to the owning session), disposition codes (CRUD), and agent-performance (snapshot + list) — contiguous monolith removal; the repository is the sole `storage` seam plus the websocket-broadcast infra seam (queue/session live updates), the service owns the not-found 404s + the post-mutation broadcasts, the controller keeps the garage-required 400 + the Zod 400s (`sanitizeZodError`) + the request-body strips + the `201`/`204` + the exact per-handler `{ message }` 500s; every route keeps `isAuthenticated`, the per-route `callCenterLimiter` rate limit was relocated into the module, and all `requireResourceOwnership` guards (incl. the `idParam:'queueId'`/`'sessionId'` parent-scoped ones) are preserved; the h1-central-guard-rollout cross-tenant integration test passes unchanged as the behavioral safety net)) · notifications ✅ (the in-app notification CRUD (list/unread-count/get/create/patch/read/delete) + the test endpoint + the **13 email/SMS notification triggers** + the customer-facing `/my/notifications` list/read — non-contiguous monolith removal (core block + the two `/my/notifications` handlers + the 13 inline trigger `z.object` schemas relocated into the module); the repository is the sole `storage` seam plus the `emailService`/`smsService` template-build + send seams, the service owns the not-found 404s + the create/test defaults + the trigger orchestration + the recipient-scoped `/my` read/mark, the controller keeps the `201`/`204` + the Zod 400s (`z.ZodError`→`sanitizeZodError`) + the exact per-handler success/500 bodies and **generates the 13 near-identical trigger handlers from a config table** (5 email + 8 sms, each `{schema, template, category, metaType, ok, err}`); `requireResourceOwnership({table:'notifications'})` preserved on the `:id` routes, `unread-count` registered before `:id`; the h2-ownership-guards + marketplace integration suites pass unchanged as the behavioral safety net)) · integrations ✅ (the sixteen `/api/integrations/*` handlers — integration-connection CRUD + sync-logs, Google-Calendar sync/update/delete, Gmail send ×4, and the accounting + OBD reads + not-configured stubs — contiguous monolith removal (+ the two now-orphaned `insertIntegration*Schema` imports dropped); the repository is the sole `storage` seam plus the dynamically-imported `googleCalendar.js`/`gmail.js` provider seams, the service owns the garage-scoped connection not-found 404 + the sync orchestration (call the provider → write an `integration_sync_logs` row recording success/failure) + the stub bodies, the controller keeps the connection `res.json` (no 201) + the exact per-handler 500s (incl. the `{message, error}` shape on the create/sync handlers) and preserves the legacy behavior of letting a connection-create Zod error surface as a 500; `requireResourceOwnership({table:'integration_connections'})` preserved on the `:id` mutations)) · iot ✅ (the fourteen `/api/iot/*` handlers — vehicle sensors CRUD, sensor readings (record + list + latest-per-vehicle), vehicle anomalies, alerts (list/get/acknowledge/resolve), and the dashboard summary — contiguous monolith removal (+ the two orphaned `insertIoTSensor*Schema` imports dropped); the repository is the sole `storage` seam, the service **centralizes the repeated vehicle-ownership 403 (`Access denied`)** via a private `assertVehicleOwned` helper + the not-found 404s + the date-format 400s + the resolve pre-checks (resolution-required 400, foreign-job-card 403) + the dashboard aggregation, the controller keeps the Zod 400s (`sanitizeZodError`) on the sensor/reading bodies + the `201`s + the exact per-handler 500s; the parent-scoped `requireResourceOwnership({table:'iot_sensors'|'iot_alerts', parent:{vehicles}})` and the `vehicles` idParam guards preserved on the `:id`/vehicle routes)) · dynamic-pricing ✅ (the sixteen `/api/dynamic-pricing/*` handlers — market pricing data CRUD, vehicle pricing factors CRUD, pricing suggestions (list/get/create/accept-reject/delete), the price calculator, and the static service-type + vehicle-class catalogues — contiguous monolith removal; the repository is the sole `storage` seam (market-data / vehicle-factor / suggestion CRUD + the `calculateDynamicPrice` engine), the service owns the garage-required 400 on the suggestions list + the not-found 404 on a single suggestion + the accept transition (stamping `acceptedBy`/`acceptedAt`) + the service-type-required 400 on calculate + the static catalogues, the controller stays thin (the `201`s, the `{ success: true }` delete bodies, the exact per-handler `{ message: error.message }` 500s with the same console labels); the `requireResourceOwnership({table:'market_pricing_data'|'vehicle_pricing_factors'|'dynamic_pricing_suggestions'})` guards preserved on the `:id` mutations)) · feedback ✅ (the eleven `/api/feedback/*` handlers — submit (Zod-validated), per-job-card / per-technician / filtered lookups, analytics, get-by-id, respond / flag / unflag, and the single + bulk OpenAI sentiment analysis — contiguous monolith removal (+ the now-orphaned `insertServiceFeedbackSchema` import dropped); the repository is the sole `storage` seam **plus the OpenAI sentiment-analysis seam** (lazy `import('openai')`), the service owns the async technician-summary refresh after a submission + the not-found 404s + the response/reason-required 400s + the no-comments-to-analyze 400 + the single/bulk sentiment orchestration (bulk caps at 20 unanalysed, records per-item success/failure), the controller keeps the `insertServiceFeedbackSchema` Zod 400 (`sanitizeZodError`) + the query→filters parsing + the exact per-handler `{ message }` 500 strings; the `requireResourceOwnership` guards preserved — `{table:'job_cards', idParam:'jobCardId'}` on the job-card lookup, `{table:'service_feedback', idParam:'technicianId', parent:{job_cards}}` on the technician lookup, and `{table:'service_feedback', parent:{job_cards}}` on the `:id` routes — with `analytics` registered before `:id`)) · compliance ✅ (the ten `/api/compliance/*` handlers — environmental records (create/list/analytics) + the policy/audit/task management surface — **non-contiguous** monolith removal across two blocks (+ the inline `complianceRecordSchema` relocated into the module and the three orphaned `insertCompliance*Schema` imports dropped); the repository is the sole data seam across **two backends** — the `phase6-compliance-service` for environmental records (Drizzle) and the `storage` facade for policies/audits/tasks, the service owns the environmental field mapping (string→number coercions for the decimal columns, `recordDate`→`Date`) + the analytics trailing-year default range + the passthroughs, the controller **preserves the two distinct legacy response conventions verbatim** — environmental: `201` bare-body create + `{ message }` 500 strings (with console labels); policies/audits/tasks: `{ data }` envelope + `{ error: message }` 500s — both Zod `400` via `sanitizeZodError`; `requireResourceOwnership({table:'compliance_tasks'})` preserved on the `tasks/:id/complete` route))

Cross-cutting layers (caching E11, telemetry/metrics/audit E13, CQRS E6 for
accounting/reporting/analytics) are introduced as the domains that need them are
migrated — not up front.

## Quality gates (per step)

- `npm run typecheck` — 0 errors
- `npm run lint` + `npm run lint:arch` — clean
- `npm run test` — green (unit + integration)
- `npm run db:check-drift` — green when schema is touched
- No unresolved circular dependencies; dependency direction respected.
