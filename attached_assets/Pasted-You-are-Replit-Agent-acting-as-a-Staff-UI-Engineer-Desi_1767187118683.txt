You are Replit Agent acting as a Staff UI Engineer + Design System Lead.

OBJECTIVE
Redesign and refactor the entire SaaS web app UI/UX to match the provided brand system and apply BOTH Dark Mode + Light Mode across ALL pages, sub-pages, nested flows, and UI states (forms, tables, modals, drawers, charts, empty states). No screen should remain unstyled or inconsistent.

NON-STOP EXECUTION RULES
- Do not ask me any questions.
- If ambiguity exists, make the best professional assumption and continue.
- Do not leave TODOs or placeholders.
- Replace inconsistent styles with reusable components.
- Your job ends only when every discovered route/screen is updated and verified in both themes.

INPUTS
You have multiple archives uploaded in the workspace:
- New folder.rar
- New folder (2).rar
- New f.rar
- New fol.rar
Also, there may be docs.zip and/or screenshots.zip available.
You MUST extract and use them.

STEP 0 — EXTRACT & IDENTIFY THE REAL APP
1) Extract all .rar archives into a single workspace folder, e.g. /workspace/app-src/
2) Deduplicate and identify the real runnable app (look for package.json / vite.config / next.config / src/ / app/).
3) Ensure the app runs locally (npm install, npm run dev).
4) Create a short inventory file at:
   /docs/00_app_inventory.md
   Include:
   - framework (React/Vite/Next/etc.)
   - routes/pages location
   - UI library used (if any)
   - where global styles live
   - where charts/tables live

DESIGN SYSTEM (STRICT – NO DEVIATION)
COLOR TOKENS
Primary:
- Gradient Blue: #0A5ED7 → #0BB3FF
- Deep Navy: #0B1F3B

Neutrals (Dark):
- Background Dark: #0E1117
- Surface Card: #151A23
- Border: #232A36
- Text Primary: #E6EAF0
- Text Muted: #9BA4B0

Accent:
- Action Orange: #F97316 (use sparingly: warnings, decisions, critical CTAs ONLY)

RULES:
- Blue = data
- Orange = decisions / warnings / CTAs
- Do NOT introduce random colors.
- Charts must use brand-safe palette variants (blue shades + gray + orange only for warnings).
- Buttons: primary = gradient, secondary = outline.

TYPOGRAPHY
- Primary: Inter (fallback SF Pro / IBM Plex Sans)
- Metrics/Numbers: JetBrains Mono or Inter Mono
Hierarchy:
- H1 bold + tight tracking
- H2–H4 medium
- Body regular high contrast
- Labels uppercase, small, muted

CORE LAYOUT SYSTEM
- Left vertical sidebar (icon-first, collapsible)
- Top bar: search + notifications + user menu
- Main content: modular cards
- Optional right insight rail: activity/alerts/team (collapsible)

COMPONENTS (MUST BUILD / STANDARDIZE)
- AppShell layout (Sidebar + Topbar + Content + optional RightPanel)
- Button (primary gradient, secondary outline, danger orange)
- Card (subtle depth, no heavy shadows)
- Badge / Status pill (blue/gray/orange)
- DataTable (sticky header, hover actions, sorting/filtering hooks)
- Form inputs (states: default/hover/focus/error/disabled)
- Modal (centered, compact)
- Drawer (right side filters / details)
- Toast (top-right, auto-dismiss)
- EmptyState (icon + short sentence)
- Loading (thin linear progress bar only; no spinners)
- Charts (line/area/bar with left→right animation)

THEMES (CRITICAL)
Implement a real theme system:
- Use CSS variables (design tokens) for ALL colors.
- Support DARK + LIGHT themes.
- Add theme toggle in Topbar.
- Persist preference in localStorage.
- Ensure contrast is valid in both themes.
Light mode guidance:
- Background: soft off-white
- Cards: white
- Borders: light gray
- Same blue/orange semantics.

REQUIRED SCREENS TO COVER (100%)
You must redesign and refactor ALL existing screens, including but not limited to:
1) Login / Onboarding
2) Main Dashboard (KPIs, trends, status blocks, recent activity)
3) Analytics / Data pages (full-width charts, daily/weekly/monthly toggle, right filter drawer, export buttons PDF/CSV)
4) Workflow / Automation Builder (horizontal pipeline or nodes; step cards; S-curve flow feel)
5) Tables & Management pages (dense readable tables, sticky headers, hover inline actions, status pills)
6) Settings / Admin (split layout, minimal color, control room)

MANDATORY COVERAGE PROCESS (NO SKIPPING)
1) ROUTE / SCREEN INVENTORY
- Enumerate every route/page and list them in:
  /docs/ui-coverage-checklist.md
  Columns:
  - route/path
  - screen name
  - components used
  - dark verified (yes/no)
  - light verified (yes/no)

2) SCREEN-BY-SCREEN APPLICATION
For every discovered route:
- Apply AppShell layout
- Replace all raw styling with tokens/components
- Ensure forms/tables/modals/drawers/charts match system
- Verify both themes visually and functionally
- Mark checklist row as verified

3) STATES & EDGE CASES
For every major form/table:
- empty state
- loading state
- error state
- success state
- disabled state
- long text overflow
- responsive layout (mobile)

IMPLEMENTATION TECH REQUIREMENTS
- Prefer React + Tailwind + shadcn/ui if compatible.
- If Tailwind exists: implement tokens via :root + [data-theme="dark|light"] and map to Tailwind via CSS variables.
- Refactor duplicated UI into shared components.
- Remove hard-coded colors and inline style junk.

DELIVERABLES
You MUST produce:
1) Fully redesigned UI across the entire app
2) Dark + Light themes with toggle + persistence
3) /docs/ui-coverage-checklist.md (complete, no missing pages)
4) /docs/design-system.md (tokens, typography, components, rules)
5) /docs/component-map.md (what components exist + where used)
6) A final verification note:
   - list any screens you could not render and why (only if truly blocked)

EXECUTION ORDER
1) Extract archives + run app
2) Build tokens + theme toggle
3) Build/refactor AppShell + shared components
4) Apply to: Login → Dashboard → Tables → Analytics → Workflow → Settings → Remaining pages
5) Final coverage audit: every route checked, both themes verified

START NOW. DO NOT STOP UNTIL ALL ROUTES ARE DONE AND VERIFIED IN BOTH THEMES.
