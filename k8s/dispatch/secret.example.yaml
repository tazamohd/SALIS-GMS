# Template only — do not commit real credentials. Create the real secret with:
#   kubectl create secret generic dispatch-secrets \
#     --namespace salis-dispatch \
#     --from-literal=DISPATCH_DATABASE_URL='postgresql://user:pass@host:5432/salis_dispatch'
# or via your cluster's sealed-secrets / external-secrets pipeline (see
# .claude/skills/kubernetes-specialist/references/gitops.md).
apiVersion: v1
kind: Secret
metadata:
  name: dispatch-secrets
  namespace: salis-dispatch
type: Opaque
stringData:
  DISPATCH_DATABASE_URL: "postgresql://REPLACE_ME:REPLACE_ME@REPLACE_ME:5432/salis_dispatch"
