{
  "name": "salis-auto-public-portal",
  "private": true,
  "version": "0.1.0",
  "description": "SALIS AUTO customer-facing public portal \u2014 static pages assembled from shared partials.",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "serve": "node build.mjs && cd dist && python3 -m http.server 8080",
    "fonts": "node tools/fetch-fonts.mjs"
  }
}
